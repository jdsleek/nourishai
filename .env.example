# Get your key from https://console.anthropic.com/
# This key is used ONLY server-side inside Next.js API routes — it is never bundled into the browser.
ANTHROPIC_API_KEY=

# Optional: override the Claude model (defaults to claude-3-5-sonnet-latest)
# ANTHROPIC_MODEL=claude-3-5-sonnet-latest
